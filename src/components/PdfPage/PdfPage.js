import React from "react";

const PdfPage = () => {
  return (
    <div className="PdfPage_page">
      {/* <header className="App-header">
        <Navbar bg="dark" variant="dark" expand="lg" expanded={expanded}>
          <Container>
            <Navbar.Brand href="#home">Marisa Vukas</Navbar.Brand>
            <Navbar.Toggle
              aria-controls="basic-navbar-nav"
              onClick={handleToggle}
              isRunning={expanded}
            />
            <Navbar.Collapse id="basic-navbar-nav">
              <Nav className="me-auto mt-3 mb-3">
                <Nav.Link href="#home" onClick={() => setExpanded(false)}>
                  Home
                </Nav.Link>
                <Nav.Link href="#about" onClick={() => setExpanded(false)}>
                  About
                </Nav.Link>
                <Nav.Link href="#projects" onClick={() => setExpanded(false)}>
                  Projects
                </Nav.Link>
                <Nav.Link href="#contact" onClick={() => setExpanded(false)}>
                  Contact
                </Nav.Link>
              </Nav>
            </Navbar.Collapse>
          </Container>
        </Navbar>
      </header> */}
    </div>
  );
};

export default PdfPage;
